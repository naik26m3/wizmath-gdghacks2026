rules_version = '2';

// Permissive rules for testing only - allow all reads/writes during emulation
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
